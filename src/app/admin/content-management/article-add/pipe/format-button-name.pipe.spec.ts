import { FormatButtonNamePipe } from './format-button-name.pipe';

describe('FormatButtonNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatButtonNamePipe();
    expect(pipe).toBeTruthy();
  });
});
